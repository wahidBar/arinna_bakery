version: '3.8'

services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: arinna_bakery_app
    ports:
      - "8000:80"
    env_file:
      - .env
    environment:
      - PORT=80
    restart: unless-stopped
