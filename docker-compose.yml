services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: arinna_bakery_app
    ports:
      - "${PORT:-8000}:8080"
    env_file:
      - .env
    restart: unless-stopped
